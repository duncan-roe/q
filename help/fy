  \/ F Y - T O K E N   Y C H A N G E A L L \/

Functions exactly as Y-YCHANGEALL except that the string being searched
for is searched for as a TOKEN, and is never treated as a regular expression.

Format
 see YCHANGEALL

Parameters
 see YCHANGEALL (except FY can't take a <match index>)

File Position
 see YCHANGEALL

Notes
 See FLOCATE for notes on what constitutes a token.
